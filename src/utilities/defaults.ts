const defaultData = {
  banner: [],
  bannerForced: false,
  bannerVersion: 0,
} as BucketDataResponse;

export const emptyBucketDataResponse = {
  data: defaultData,
};

export const emptyBucketData = defaultData;
